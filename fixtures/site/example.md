# Example Keyword Guide

This is placeholder fixture content for the page at `/example/`.

It intentionally lacks a clear answer to the user's search intent, so the
SEO loop's fixture end-to-end test has a real gap to close.
